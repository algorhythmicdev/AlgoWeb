# Environment variables template for AlgoWeb
# Copy this file to .env and fill in your actual values

# PUBLIC variables (exposed to client-side code)
# PUBLIC_API_URL=https://api.example.com

# PRIVATE variables (server-side only)
# DATABASE_URL=
# API_KEY=

# Strapi CMS Configuration
# Development: http://localhost:1337
# Production: https://cms.yourdomain.com
PUBLIC_STRAPI_URL=http://localhost:1337

# Strapi API Token (generate in Strapi admin: Settings > API Tokens)
# Used for server-side authenticated requests
STRAPI_API_TOKEN=
# JWT Secret (must match Strapi's JWT_SECRET for token validation)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=
