/**
 * Admin Dashboard Page Configuration
 * Disable prerendering for admin pages
 */

// Disable prerendering - admin pages require authentication
export const prerender = false;
export const ssr = true;
